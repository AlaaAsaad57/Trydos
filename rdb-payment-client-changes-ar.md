<div dir="rtl" lang="ar">

# الدفع عبر RDB: ما الذي يتغيّر لمطوّري تطبيق Trydos وموقع الويب

التاريخ: 2026-09-15. فرع الباك-إند: `ticket/rdb-merchant-payment-requests`
(الـ commits: `cccb8cbc` و `3b65077b`). كلمة `{base}` أدناه تعني عنوان
API الخاص ببيئتكم (مثال: `https://market_staging.antiksef.online`).

## 1. الملخّص

الدفع بمحفظة RDB لم يعد يتم من طرف العميل (التطبيق أو الموقع). العميل لا
يتصل بـ RDB أبداً ولا يحمل أي مفتاح أو توقيع خاص بـ RDB. بدلاً من ذلك:

1. العميل يطلب من باك-إند Trydos بدء عملية دفع RDB (طلب واحد).
2. الباك-إند ينشئ طلب الدفع على RDB ويعيد رابط deep link ومحتوى QR ورمزاً قصيراً.
3. الزبون يدفع داخل تطبيق RDB (عبر الرابط) أو بمسح الـ QR.
4. العميل يستعلم دورياً (polling) من نقطة واحدة في Trydos حتى تصبح الحالة
   `paid` (أو تنتهي بـ `expired` أو `cancelled` أو `failed`).
5. الطلبات (orders) تُنشأ فقط بعد `paid`. الاسترجاعات تعود لمحفظة RDB
   تلقائياً؛ لا عمل مطلوب من طرف العميل.

## 2. ما يجب حذفه من طرف العميل

- أي استدعاء لـ RDB `POST /merchant/checkout`.
- أي مفتاح تاجر أو سر أو كود توقيع HMAC، وأي هيدر من:
  `X-Merchant-Api-Key` أو `X-Signature` أو `X-Timestamp`.
- أي منطق يحسب مبلغ RDB في العميل. الباك-إند هو من يحدد المبلغ
  (إجمالي السلة ناقص الخصم ناقص جزء المحفظة الاختياري).

## 3. طلب الدفع الجديد (التطبيق والموقع)

`POST {base}/api/v1/customer/order/checkout/rdb`

الهيدرز: نفس هيدرز طلبات الدفع الحالية (`Authorization: Bearer <customer token>`،
`Accept: application/json`، `lang`، `country`). يجب أن يكون الزبون موثّقاً
(هاتف أو بريد) كما في كل طرق الدفع.

جسم الطلب (نفس حقول طرق الدفع الأخرى):

| الحقل | النوع | مطلوب | ملاحظات |
|---|---|---|---|
| `address_id` | int | نعم | يجب أن يكون العنوان الافتراضي للزبون، كما هو الحال اليوم |
| `order_note` | string | لا | |
| `pay_by_wallet` | 0 أو 1 | لا | **يُتجاهَل.** محفظة Trydos لا تحمل مالاً؛ RDB يحصّل دائماً المبلغ الكامل (إجمالي السلة ناقص الخصم). توقفوا عن إرساله مع `rdb`. |

### 3.1 نجاح: تم إنشاء طلب دفع RDB (HTTP 200)

<div dir="ltr">

```json
{
  "isSuccessful": true,
  "hasContent": true,
  "code": 200,
  "message": "rdb_payment_request_created",
  "detailed_error": null,
  "data": {
    "request_reference": "0f3c1c1e-6d0a-4b8b-9b7e-2f6a0f9c1d22",
    "status": "awaiting_payment",
    "rdb_request_id": "665a…",
    "request_code": "REQ8F3K2M1Q",
    "short_code": "12345678",
    "deep_link": "rdb://pay/REQ8F3K2M1Q",
    "qr_payload": "https://pay.rdb.example/r/v1/REQ8F3K2M1Q",
    "amount": "100.00",
    "currency": "USD",
    "expires_at": "2026-09-15T14:30:00+00:00",
    "paid_at": null,
    "receipt_number": null,
    "failure_reason": null,
    "order_ids": []
  }
}
```

</div>

ماذا تفعل به:

- التطبيق: افتح `deep_link` إذا كان تطبيق RDB مثبتاً؛ وإلا اعرض `qr_payload`
  كصورة QR واعرض `short_code` للإدخال اليدوي.
- الموقع: اعرض `qr_payload` كصورة QR واعرض `short_code`.
- اعرض `amount` و `currency` كنصوص كما وصلت (لا تحوّلها إلى float للعرض).
- احتفظ بـ `request_reference`؛ هو المفتاح للاستعلام الدوري وللإلغاء.
- أوقف الاستعلام عند `expires_at` وأخبر الزبون أن الطلب انتهى.

### 3.2 لا توجد حالة "المحفظة غطّت كل شيء"

استدعاء `checkout/rdb` يجيب دائماً بكائن طلب الدفع الموضح في 3.1 (أو بخطأ).
لا ينشئ طلبات مباشرة أبداً ولا يستخدم رصيد محفظة Trydos أبداً، مهما كانت قيمة
`pay_by_wallet`. وينطبق الأمر نفسه الآن على طريقتي الدفع عند الاستلام والعملات
الرقمية: لم يعد أي دفع يأخذ جزءاً من محفظة Trydos.

### 3.3 الأخطاء

| HTTP | متى | الجسم |
|---|---|---|
| 400 | خطأ تحقق (مثال: `address_id` ليس العنوان الافتراضي) | أخطاء التحقق |
| 403 | مشكلة في السلة أو قاعدة عمل: كمية غير متوفرة، السلة غير متاحة، لا يوجد ما يُدفع، تعذّر الوصول إلى RDB | `message` يشرح السبب؛ يمكن إعادة المحاولة بعد معالجة السبب |
| 409 | لدى الزبون طلب RDB معلّق بالفعل | `message`: "You already have a pending RDB payment. Complete or cancel it first." ← وجّه الزبون إلى الطلب الحالي (استعلم عنه) أو اعرض الإلغاء |
| 500 | خطأ غير متوقع | رسالة عامة؛ أعد المحاولة لاحقاً |

## 4. الاستعلام الدوري عن حالة الطلب

`GET {base}/api/v1/customer/order/rdb-request/{request_reference}`

نفس هيدرز المصادقة. يعيد نفس كائن `data` في 3.1 مع الحالة الحالية `status`:

| `status` | المعنى | إجراء العميل |
|---|---|---|
| `awaiting_payment` | لم يُدفع بعد | استمر بالاستعلام كل 3 إلى 5 ثوانٍ حتى `expires_at` |
| `paid` | تم الدفع وأُنشئت الطلبات | `order_ids` ممتلئة وقد يكون `receipt_number` موجوداً؛ انتقل إلى شاشة الطلب عبر نقاط الطلبات الحالية |
| `expired` | لم يُدفع قبل انتهاء المهلة | أخبر الزبون؛ السلة أصبحت قابلة للتعديل؛ يمكنه إعادة الدفع |
| `cancelled` | أُلغي من الزبون أو من RDB | مثل `expired` |
| `failed` | تم الدفع لكن تعذّر إنشاء الطلب (مثلاً تغيّرت السلة) أو فشل RDB | اعرض `failure_reason`؛ المبلغ يُسترجع إلى محفظة RDB تلقائياً؛ السلة أصبحت قابلة للتعديل |

المرجع الذي يخص زبوناً آخر، أو غير الموجود، يعيد 404.

## 5. إلغاء طلب معلّق

`POST {base}/api/v1/customer/order/rdb-request/{request_reference}/cancel`

| HTTP | المعنى |
|---|---|
| 200 | أُلغي؛ `data.status` تصبح `cancelled`؛ السلة أصبحت قابلة للتعديل |
| 409 | الطلب مدفوع بالفعل ولا يمكن إلغاؤه (استعلم عنه؛ الطلبات موجودة) |
| 404 | مرجع غير معروف |
| 500 | تعذّر الوصول إلى RDB؛ أعد المحاولة |

أضف زر إلغاء في شاشة الدفع ليتمكن الزبون من العودة إلى السلة.

## 6. السلة مقفلة أثناء وجود طلب معلّق

طالما أن هناك طلباً بحالة `awaiting_payment`، كل تعديل على السلة يعيد **409**:

- `POST /api/v1/cart/add` و `/api/v1/cart/update` و `/api/v1/cart/remove`
  و `/api/v1/cart/remove-all` و `/api/v1/cart/convert_to_old`
- `POST /api/v1/old-cart/convert_to_cart` و `/api/v1/old-cart/hide`
- `POST /api/v1/customer/order/checkout/{أي طريقة}`

جسم الـ 409:

<div dir="ltr">

```json
{
  "isSuccessful": false,
  "code": 409,
  "message": "Your cart is locked until the pending RDB payment is completed or cancelled.",
  "detailed_error": [ { "message": "…" } ],
  "data": {
    "rdb_request_reference": "0f3c1c1e-…",
    "expires_at": "2026-09-15T14:30:00+00:00"
  }
}
```

</div>

إجراء العميل: اعرض "لديك عملية دفع جارية" مع زرّين: "متابعة الدفع" (افتح
شاشة الطلب بـ `rdb_request_reference`) و "إلغاء الدفع" (القسم 5). طلبات
القراءة (`GET`) لا تُقفل أبداً.

## 7. ما لا يتغيّر

- قائمة الطلبات، تفاصيل الطلب، التتبّع، إلغاء الطلب، إلغاء عنصر: نفس النقاط.
  للطلبات المدفوعة عبر RDB يرسل الباك-إند الاسترجاع إلى محفظة RDB؛ علامة
  "تم إرجاع المبلغ" على الطلب تُضبط عندما يؤكد RDB، وقد يستغرق ذلك وقتاً في
  الاسترجاعات الكبيرة. لا شيء يُضاف في العميل.
- طرق الدفع: المحفظة، الدفع عند الاستلام، العملات الرقمية، البطاقة: بلا تغيير.
- نقطة الخادم القديمة `POST /api/rdb/order/payment` حُذفت؛ لم تكن نقطة عميل أصلاً.

## 8. ملاحظة لموقع الويب (متجر الزبائن)

الموقع يستخدم نفس نقاط `/api/v1` التي يستخدمها التطبيق، لذا تنطبق عليه الأقسام
من 3 إلى 7 دون تغيير، بما فيها قفل السلة في القسم 6.

مسارات المتجر القديمة المولَّدة من الخادم في `routes/web_routes/web.php` ميتة:
متحكماتها لم تعد موجودة. في 2026-09-15 حذف الباك-إند 83 مساراً منها لا يشير
إليها شيء. بقيت بضعة *أسماء* مسارات فقط لأن كوداً حياً ما زال يبني روابط بها
(مثل روابط المنتجات في ردود الـ API، صفحة الصيانة، رابط رسالة السلة المتروكة،
قوالب البريد، ونموذج تسجيل البائع)؛ وهي ليست نقاطاً يجب أن يستدعيها الموقع.
لا تستدعِ أي مسار من `web.php` من الموقع؛ استخدم `/api/v1`.

## 9. قائمة اختبار لفرق العميل

1. الدفع بطريقة `rdb` ← يظهر الرابط / الـ QR، ويظهر `amount` كنص.
2. الدفع في بيئة RDB التجريبية ← الاستعلام يتحول إلى `paid` وتظهر الطلبات.
3. ترك الطلب حتى ينتهي ← الحالة `expired` والسلة قابلة للتعديل مجدداً.
4. الإلغاء من شاشة الدفع ← الحالة `cancelled` والسلة قابلة للتعديل.
5. محاولة الإضافة إلى السلة أثناء التعليق ← معالجة 409 بالزرّين.
6. محاولة الدفع مجدداً أثناء التعليق ← معالجة 409 بالطريقة نفسها.
7. `pay_by_wallet = 1` مع أي رصيد في المحفظة ← ما زال يظهر QR / رابط بالمبلغ الكامل؛ لا يُنشأ طلب ولا يُمسّ الرصيد.

</div>
